import { FilteringDataPipe } from './filtering-data.pipe';

describe('FilteringDataPipe', () => {
  it('create an instance', () => {
    const pipe = new FilteringDataPipe();
    expect(pipe).toBeTruthy();
  });
});
